# ================================================================
# HR PORTAL CONFIGURATION
# ================================================================
# TODO: To be implemented in Phase 2, Week 9-12
# ================================================================

# ----------------------------------------------------------------
# Service Configuration
# ----------------------------------------------------------------
NEXT_PUBLIC_APP_NAME=Werkflow HR Portal
NEXT_PUBLIC_APP_VERSION=1.0.0

# Port for this frontend (HR Portal)
PORT=4001

# ----------------------------------------------------------------
# Backend API URLs
# ----------------------------------------------------------------
# Flowable Engine Service
NEXT_PUBLIC_ENGINE_API_URL=http://localhost:8081/api

# HR Service
NEXT_PUBLIC_HR_API_URL=http://localhost:8082/api

# Admin Service
NEXT_PUBLIC_ADMIN_API_URL=http://localhost:8083/api

# ----------------------------------------------------------------
# NextAuth Configuration
# ----------------------------------------------------------------
NEXTAUTH_URL=http://localhost:4001
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# ----------------------------------------------------------------
# Keycloak Configuration
# ----------------------------------------------------------------
# OAuth2 Client Credentials
KEYCLOAK_CLIENT_ID=werkflow-hr-portal
KEYCLOAK_CLIENT_SECRET=your-client-secret-from-keycloak

# OAuth2 Issuer Configuration (Docker Multi-URL Strategy)
# KEYCLOAK_ISSUER: Used by NextAuth for server-side operations
#   - OIDC discovery (.well-known/openid-configuration)
#   - Token validation and refresh
#   - Userinfo endpoint calls
# For local development outside Docker: http://localhost:8090/realms/werkflow
# For Docker containers: http://keycloak:8080/realms/werkflow
KEYCLOAK_ISSUER=http://localhost:8090/realms/werkflow

# KEYCLOAK_ISSUER_BROWSER: Used for OAuth redirects that happen in user's browser
#   - Authorization endpoint (login page redirect)
#   - Must be accessible from user's browser (not from container)
# For local development: http://localhost:8090/realms/werkflow
# Leave blank to use same as KEYCLOAK_ISSUER
KEYCLOAK_ISSUER_BROWSER=http://localhost:8090/realms/werkflow

# ----------------------------------------------------------------
# Feature Flags
# ----------------------------------------------------------------
NEXT_PUBLIC_ENABLE_EMPLOYEE_DASHBOARD=true
NEXT_PUBLIC_ENABLE_LEAVE_MANAGEMENT=true
NEXT_PUBLIC_ENABLE_PERFORMANCE_REVIEWS=true
NEXT_PUBLIC_ENABLE_ONBOARDING=true
