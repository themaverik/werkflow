# Connector Schema (generated mirror)

The JSON Schema files in this directory are **generated mirrors**. The
canonical source lives in `werkflow-enterprise`
(`services/admin/src/main/resources/schemas/connector/v1/`).

Do not edit these files here — an automated sync will overwrite local changes.

To propose a change, open a GitHub issue or pull request describing it. A
maintainer ports accepted changes to the canonical schema, which then syncs
back down automatically.
